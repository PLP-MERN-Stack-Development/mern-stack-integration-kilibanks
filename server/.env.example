# Server environment variables example
# Copy this to `.env` and fill values before running the server

PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/mern-blog
NODE_ENV=development
JWT_SECRET=your_jwt_secret_here

# Optional: storage path for uploaded files
UPLOAD_DIR=uploads
