# Client environment variables for development
# `VITE_API_URL` can point to backend API (e.g. http://localhost:5000/api)
# If unset, Vite dev server proxy will forward `/api` to the backend.

VITE_API_URL=
